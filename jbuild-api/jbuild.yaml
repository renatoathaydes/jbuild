imports:
    - ../errorprone.yaml
    - ../build_properties.yaml

group: com.athaydes.jbuild
module: jbuild-api
version: 0.8.0
description: JBuild Java API for extending the jb tool.

javac-args: [ "--release={{versions.java}}" ]

source-dirs: [ src ]
