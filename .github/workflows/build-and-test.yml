name: Build And Test on All OSs
on: [push, pull_request]
jobs:
  build:
    strategy:
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
        java: [ 11, 17, 21 ]
        include:
            - os: ubuntu-latest
              oskind: linux
            - os: macos-latest
              oskind: macos-arm64
            - os: windows-latest
              oskind: windows
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: ${{ matrix.java }}

      - name: Set JB_HOME
        # Use an absolute path with forward slashes so the value works both in
        # bash (including Git Bash on Windows, where GITHUB_WORKSPACE contains
        # backslashes) and when read by the JVM.
        run: echo "JB_HOME=${GITHUB_WORKSPACE//\\//}/jb" >> "$GITHUB_ENV"
        shell: bash

      - name: Download jb
        uses: dsaltares/fetch-gh-release-asset@master
        with:
          repo: 'renatoathaydes/jb'
          version: 'tags/0.15.0'
          file: 'jb-0.15.0-${{ matrix.oskind }}.tar.gz'
          target: jb.tar.gz

      - name: Install jb
        run: |
              mkdir -p "$JB_HOME"
              tar xzf jb.tar.gz -C "$JB_HOME"
              "$JB_HOME/bin/jb" -v
        shell: bash

      - name: Build and test jbuild-classfile-parser
        run: $JB_HOME/bin/jb -d -p jbuild-classfile-parser/test test
        shell: bash

      - name: Build and test JBuild main artifact
        run: $JB_HOME/bin/jb -d -p src/test test
        shell: bash

      - name: Build and execute integration tests
        run: $JB_HOME/bin/jb -p src/intTest test
        shell: bash

      - name: Test Report
        uses: dorny/test-reporter@v1
        if: success() || failure()
        with:
          name: JUnit Tests
          path: build/test-reports/(test|intTest)/TEST-*.xml
          reporter: java-junit
